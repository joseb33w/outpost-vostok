# Godot import cache + export output
.godot/
out/

# Local toolchain / scratch (cloud-agent only)
/godot
*.tmp
*.log
verify/
playbooks/
_ref/

# Fetched / generated binary assets — run tools/fetch_assets.sh to recreate
models/
skies/
textures/
audio/

# Import metadata (regenerated by `godot --import`)
*.import
